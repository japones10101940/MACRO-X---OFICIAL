for i,v in next, require(game:GetService('ReplicatedStorage').Module.UI).CircleAction.Specs do
			if v.Name == "Rob" then
				pcall(function()
					v.Duration = 0
					v:Callback(true)
				end)
			end
		end
    wait()
    for i,v in next, require(game:GetService('ReplicatedStorage').Module.UI).CircleAction.Specs do
			if v.Name == "Rob" then
				pcall(function()
					v.Duration = 0
					v:Callback(true)
				end)
			end
		end
wait()
for i,v in next, require(game:GetService('ReplicatedStorage').Module.UI).CircleAction.Specs do
			if v.Name == "Rob" then
				pcall(function()
					v.Duration = 0
					v:Callback(true)
				end)
			end
		end
